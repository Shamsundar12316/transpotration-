// part 9 //question 13/ transpotration
var transpotration = ["car", "bus", "bike", "plane"];
for (var i = 0; i < transpotration.length; i++) {
    console.log("I would like to own a " + transpotration[i]);
}
